{
  "name": "balise-skills",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync:readme": "node scripts/sync-readme-metadata.mjs",
    "check:readme": "node scripts/sync-readme-metadata.mjs --check",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate && npm run check:readme"
  }
}
